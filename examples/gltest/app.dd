# gltest app data

require 'texture_resource.rb'

BBQ.header do
  struct Color do
    float r, 0.0
    float g, 0.0
    float b, 0.0
    float a, 0.0
  end

  struct App do
    Color clear_color
    Color quad_color
    OpenGLTexture background
  end
end
    
